class Parsing {
    public static void main (String yash[]){
        int i1=Integer.parseInt(yash[0]);
        float i2=Integer.parseInt(yash[1]); // float or double.
        float c=i1/i2; //Gives Zero , atlest one input must be float
        System.out.println(c);
       //keywords are not used as variables like bool,for,new,short etc.
    }
}
